windows_downloads_dir: "{{windows_downloads_dir}}"
database_file_path: "{{database_file_path}}"
artifact_dir: data
output_dir: "{{output_dir}}"
shows: {}
job:
  name: frieren_s2e07_4k_job
  pipelines:
    - name: frieren_s2e07_4k
      metadata:
        source: erai-raws
        show: Sousou no Frieren
        season: "2"
        episode: "7"
      input:
        path: "{{input_path}}"
      steps:
        - type: interpolate
          fps: 240
        - type: copy_tracks
          source_path: "{{input_path}}"
